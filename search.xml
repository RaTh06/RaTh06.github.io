<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>hexo部署与域名解析</title>
    <url>/2026/01/17/hexo%E9%83%A8%E7%BD%B2%E4%B8%8E%E5%9F%9F%E5%90%8D%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h2 id="创建GitHub仓库"><a href="#创建GitHub仓库" class="headerlink" title="创建GitHub仓库"></a>创建GitHub仓库</h2><ul>
<li><strong>新建仓库</strong>：在 GitHub 上创建一个名为 <code>rath06.github.io</code> 的公开仓库。</li>
<li><strong>获取地址</strong>：点击 <strong>Code</strong> 按钮，复制 <strong>SSH</strong> 链接：<code>git@github.com:rath06/rath06.github.io.git</code>。</li>
</ul>
<h2 id="域名解析-Cloudflare"><a href="#域名解析-Cloudflare" class="headerlink" title="域名解析 (Cloudflare)"></a>域名解析 (Cloudflare)</h2><ul>
<li><strong>CNAME 记录 1</strong>：添加名称为 <code>@</code> 的 CNAME 记录，目标指向 <code>rath06.github.io</code>。</li>
<li><strong>CNAME 记录 2</strong>：添加名称为 <code>www</code> 的 CNAME 记录，目标指向 <code>rath06.github.io</code>。</li>
<li><strong>SSL 模式</strong>：在 SSL/TLS 选项中，将模式设置为 <strong>“完全 (Full)”</strong>。</li>
</ul>
<h2 id="本地环境配置"><a href="#本地环境配置" class="headerlink" title="本地环境配置"></a>本地环境配置</h2><ul>
<li><p><strong>CNAME 文件</strong>：在 <code>D:\Blog\source\</code> 目录下新建文件 <code>CNAME</code>（无后缀），内容写入 <code>rath02.top</code>。</p>
</li>
<li><p><strong>修改站点配置</strong>：打开博客根目录 <code>_config.yml</code>，修改以下字段：</p>
<ul>
<li><p><code>url: https://rath02.top</code></p>
</li>
<li><p><code>deploy</code> 部分修改为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repo: git@github.com:rath06/rath06.github.io.git</span><br><span class="line">  branch: main</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p><strong>安装插件</strong>：在终端运行 <code>npm install hexo-deployer-git --save</code>。</p>
</li>
</ul>
<h2 id="部署命令"><a href="#部署命令" class="headerlink" title="部署命令"></a>部署命令</h2><p>在博客根目录依次执行：</p>
<ol>
<li><code>hexo clean</code></li>
<li><code>hexo g</code></li>
<li><code>hexo d</code></li>
</ol>
]]></content>
      <categories>
        <category>教程</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>域名解析</tag>
        <tag>GitHub</tag>
        <tag>cloudflare</tag>
      </tags>
  </entry>
  <entry>
    <title>编译原理(华保健)</title>
    <url>/2026/01/20/%E7%BC%96%E8%AF%91%E5%8E%9F%E7%90%86-%E5%8D%8E%E4%BF%9D%E5%81%A5/</url>
    <content><![CDATA[<h2 id="chapter-1"><a href="#chapter-1" class="headerlink" title="chapter 1"></a>chapter 1</h2><h3 id="编译器概述"><a href="#编译器概述" class="headerlink" title="编译器概述"></a>编译器概述</h3><h4 id="什么是编译器"><a href="#什么是编译器" class="headerlink" title="什么是编译器"></a>什么是编译器</h4><ul>
<li>编译器是一个程序</li>
<li>核心功能是把源代码翻译成目标代码<ul>
<li>源代码：C/C++，Java，C#，html，SQL，…</li>
<li>目标代码：x86，IA64，ARM，MIPS，…</li>
</ul>
</li>
</ul>
<h4 id="编译器的核心功能"><a href="#编译器的核心功能" class="headerlink" title="编译器的核心功能"></a>编译器的核心功能</h4><ol>
<li><strong>翻译阶段：</strong> 源代码 $\rightarrow$ 编译器(<strong>静态计算</strong>) $\rightarrow$ 目标程序</li>
<li><strong>执行阶段：</strong> 目标程序 $\rightarrow$ 计算机(<strong>动态计算</strong>) $\rightarrow$ 计算结果</li>
</ol>
<h4 id="编译器和解释器"><a href="#编译器和解释器" class="headerlink" title="编译器和解释器"></a>编译器和解释器</h4><blockquote>
<p>解释器也是处理程序的一种<strong>程序</strong></p>
</blockquote>
<p><img src="https://img.rath02.top/2026/01/image-20260120183150240" alt="image-20260120183150240"></p>
<h4 id="编译器简史"><a href="#编译器简史" class="headerlink" title="编译器简史"></a>编译器简史</h4><ol>
<li>历史首创</li>
</ol>
<ul>
<li>第一个编译器：计算机科学史上出现的第一个编译器是<strong>Fortran语言</strong>的编译器</li>
<li>研发时间：1954年至1957年</li>
<li>核心人物：由<code>John Backus</code>领导开发</li>
</ul>
<ol>
<li>重大影响</li>
</ol>
<ul>
<li>理论层面：推动了<strong>算法、数据结构、形式语言与自动机</strong>等理论的发展</li>
<li>实践层面：促进了<strong>软件工程、体系结构</strong>等领域的进步</li>
<li>技术架构：确立了现代<strong>编译器架构</strong>的基础</li>
</ul>
<h4 id="为什么学习编译原理"><a href="#为什么学习编译原理" class="headerlink" title="为什么学习编译原理"></a>为什么学习编译原理</h4><ul>
<li>编译原理集中体现了计算机科学的很多核心思想</li>
<li>编译器是其它领域的重要研究基础</li>
<li>编译器本身就是非常重要的研究领域<ul>
<li>新的语言设计</li>
<li>大型软件的构造和维护</li>
</ul>
</li>
</ul>
<h4 id="如何学好编译原理"><a href="#如何学好编译原理" class="headerlink" title="如何学好编译原理"></a>如何学好编译原理</h4><ol>
<li><strong>理论</strong>：深入学习掌握各种算法和数据结构。</li>
<li><strong>实践</strong>：切实提高将理论应用于解决实际问题的能力。</li>
</ol>
<h3 id="编译器结构"><a href="#编译器结构" class="headerlink" title="编译器结构"></a>编译器结构</h3><h4 id="编译器的高层结构"><a href="#编译器的高层结构" class="headerlink" title="编译器的高层结构"></a>编译器的高层结构</h4><blockquote>
<p>编译器具有非常<strong>模块化</strong>的高层结构</p>
</blockquote>
<p><img src="https://img.rath02.top/2026/01/image-20260121145358500" alt="image-20260121145358500"></p>
<h4 id="抽象的多个阶段-phase"><a href="#抽象的多个阶段-phase" class="headerlink" title="抽象的多个阶段(phase)"></a>抽象的多个阶段(phase)</h4><blockquote>
<p>编译器可看出多个阶段构成的“流水线”结构</p>
</blockquote>
<p><img src="https://img.rath02.top/2026/01/image-20260121145809762" alt="image-20260121145809762"></p>
<h4 id="一种没有优化的编译器结构"><a href="#一种没有优化的编译器结构" class="headerlink" title="一种没有优化的编译器结构"></a>一种没有优化的编译器结构</h4><p><img src="https://img.rath02.top/2026/01/image-20260121125814338" alt="image-20260121125814338"></p>
<h4 id="一种更复杂的编译器结构"><a href="#一种更复杂的编译器结构" class="headerlink" title="一种更复杂的编译器结构"></a>一种更复杂的编译器结构</h4><p><img src="https://img.rath02.top/2026/01/image-20260121130244504" alt="image-20260121130244504"></p>
<h4 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h4><ul>
<li>编译器由多个阶段组成，每个阶段都要处理不同的问题<ul>
<li>使用不同的理论、数据结构和算法</li>
</ul>
</li>
<li>因此，编译器设计中的重要问题是如何合理的划分组织各个阶段<ul>
<li>接口清晰</li>
<li>编译器容易实现、维护</li>
</ul>
</li>
</ul>
<h4 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h4><ul>
<li>源语言：加法表达式语言<code>Sum</code><ul>
<li>两种语法形式：<ul>
<li>整型数字n</li>
<li>加法e1+e2</li>
</ul>
</li>
</ul>
</li>
</ul>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">3</span><br><span class="line">3+5</span><br><span class="line">3+4+5 (结合性，加法满足左结合)</span><br></pre></td></tr></table></figure>
<ul>
<li>目标机器：栈式计算机<code>Stack</code><ul>
<li>两条指令：<ul>
<li><code>push n</code></li>
<li><code>add</code></li>
</ul>
</li>
</ul>
</li>
</ul>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">push 3 // 将3这个元素压入栈中</span><br><span class="line">push 4 // 将4这个元素压入栈中</span><br><span class="line">push 5 // 将5这个元素压入栈中</span><br><span class="line">push 6 // 将6这个元素压入栈中</span><br><span class="line">add:</span><br><span class="line">&#123;</span><br><span class="line"><span class="code">	x = pop(); // 6元素被弹出</span></span><br><span class="line"><span class="code">	y = pop(); // 5元素被弹出</span></span><br><span class="line"><span class="code">	z = x+y; // 11=6+5</span></span><br><span class="line"><span class="code">	push z; // 将11这个元素压入栈中</span></span><br><span class="line"><span class="code">&#125; </span></span><br><span class="line"><span class="code">最终栈中元素3、4、11（自底向上）</span></span><br></pre></td></tr></table></figure>
<h4 id="编译器实现"><a href="#编译器实现" class="headerlink" title="编译器实现"></a>编译器实现</h4><blockquote>
<p>任务：编译程序1+2+3到栈式计算机</p>
</blockquote>
<ol>
<li><p>阶段一：词法语法分析</p>
<p><strong>词法分析结果 (Tokens):</strong> <code>[INT:1], [PLUS:+], [INT:2], [PLUS:+], [INT:3]</code></p>
<p><strong>语法规则:</strong> 遵循加法结合律（通常为左结合）。</p>
</li>
<li><p>阶段二：语法树构建</p>
<p>根据语法分析的结果构建一颗<strong>抽象语法树</strong> (Abstract Syntax Tree)，AST 的后续遍历顺序直接决定了指令顺序。</p>
</li>
<li><p>阶段三：代码生成</p>
<p>对 AST 进行 <strong>后序遍历 (Post-order Traversal)</strong>：先访问左子树，再访问右子树，最后访问根节点。这是因为在栈式计算机中，必须先将操作数压入栈，才能执行运算符。</p>
</li>
<li><p>阶段四：代码优化</p>
<p>在编译阶段，可以进行<strong>常量折叠 (Constant Folding)</strong>。如果操作数在编译时已知，可以直接计算结果以减少运行时的指令数。</p>
</li>
</ol>
<p><img src="https://img.rath02.top/2026/01/image-20260121153239451" alt="image-20260121153239451"></p>
<p>流程结果：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th><strong>步骤</strong></th>
<th><strong>指令</strong></th>
<th><strong>栈状态 (底部 -&gt; 顶部)</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>PUSH 1</code></td>
<td><code>[1]</code></td>
<td>压入常量 1</td>
</tr>
<tr>
<td>2</td>
<td><code>PUSH 2</code></td>
<td><code>[1, 2]</code></td>
<td>压入常量 2</td>
</tr>
<tr>
<td>3</td>
<td><code>ADD</code></td>
<td><code>[3]</code></td>
<td>弹出 1, 2；压入 1+2=3</td>
</tr>
<tr>
<td>4</td>
<td><code>PUSH 3</code></td>
<td><code>[3, 3]</code></td>
<td>压入常量 3</td>
</tr>
<tr>
<td>5</td>
<td><code>ADD</code></td>
<td><code>[6]</code></td>
<td>弹出 3, 3；压入 3+3=6</td>
</tr>
</tbody>
</table>
</div>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">优化后的代码： </span><br><span class="line">PUSH 3</span><br><span class="line">PUSH 3</span><br><span class="line">ADD</span><br></pre></td></tr></table></figure>
<h2 id="chapter-2"><a href="#chapter-2" class="headerlink" title="chapter 2"></a>chapter 2</h2><h3 id="词法分析的任务"><a href="#词法分析的任务" class="headerlink" title="词法分析的任务"></a>词法分析的任务</h3><h4 id="编译器的阶段"><a href="#编译器的阶段" class="headerlink" title="编译器的阶段"></a>编译器的阶段</h4><p>源程序 —&gt; 编译器 —&gt; 目标程序</p>
<p><strong>更一进步的划分</strong>：源程序 —&gt; <code>frontend</code> —&gt; <code>IR</code> —&gt;<code>backend</code>  —&gt; 目标程序</p>
<p>其中<code>frontend</code>的划分：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[源程序] --&gt; B[词法分析器]</span><br><span class="line">    B --&gt; C[记号]</span><br><span class="line">    C --&gt; D[语法分析器]</span><br><span class="line">    D --&gt; E[抽象语法树&lt;br&gt;AST]</span><br><span class="line">    E --&gt; F[语义分析器]</span><br><span class="line">    F --&gt; G[中间表示&lt;br&gt;IR]</span><br></pre></td></tr></table></figure>
<h4 id="词法分析器的任务"><a href="#词法分析器的任务" class="headerlink" title="词法分析器的任务"></a>词法分析器的任务</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">    A[字符流] --&gt; B[词法分析器]</span><br><span class="line">    B --&gt; C[记号流]</span><br></pre></td></tr></table></figure>
<h4 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (x &gt; <span class="number">5</span>)</span><br><span class="line">    y = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    z = <span class="number">1</span>;</span><br></pre></td></tr></table></figure>
<blockquote>
<p><strong>词法分析</strong></p>
</blockquote>
<p>分析结果（Tokens）：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">// 字符流转换成对应的记号(单词)</span><br><span class="line">IF  LPAREN  IDENT(x)  GT  INT(5)  RPAREN</span><br><span class="line"><span class="code">	IDENT(y)  ASSIGN  STRING(&quot;hello&quot;)  SEMICOLON</span></span><br><span class="line"><span class="code">ELSE</span></span><br><span class="line"><span class="code">	IDENT(z)  ASSIGN  INT(1)  SEMICOLON  EOF</span></span><br></pre></td></tr></table></figure>
<h4 id="记号的数据结构定义"><a href="#记号的数据结构定义" class="headerlink" title="记号的数据结构定义"></a>记号的数据结构定义</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义 Token 的种类（枚举类型）</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">kind</span> &#123;</span> IF, LPAREN, ID, INTLIT, ... &#125;; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义 Token 结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">token</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">enum</span> <span class="title">kind</span> <span class="title">k</span>;</span>     <span class="comment">// Token 类型</span></span><br><span class="line">    <span class="type">char</span> *lexeme;    <span class="comment">// 对应的词素（单词文本）</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>对于源代码片段：<code>if ( x &gt; 5 )</code></p>
<p>词法分析器将其提取并转换为如下的 <code>token</code> 序列：</p>
<ol>
<li><code>token { k = IF, lexeme = 0 }</code>;</li>
<li><code>token { k = LPAREN, lexeme = 0 };</code></li>
<li><code>token { k = ID, lexeme = &quot;x&quot; };</code></li>
</ol>
<p>注：对于关键字，有时 lexeme 可为空或指向特定字符串。</p>
<h4 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h4><p>词法分析器的任务：字符流到记号流</p>
<ul>
<li><strong>字符流：</strong><ul>
<li>和被编译的语言密切相关（ASCII, Unicode, or …）</li>
</ul>
</li>
<li><strong>记号流：</strong><ul>
<li>编译器内部定义的数据结构，编码所识别出的词法单元</li>
</ul>
</li>
</ul>
<h3 id="词法分析器的手工构造"><a href="#词法分析器的手工构造" class="headerlink" title="词法分析器的手工构造"></a>词法分析器的手工构造</h3><h4 id="词法分析的实现方法"><a href="#词法分析的实现方法" class="headerlink" title="词法分析的实现方法"></a>词法分析的实现方法</h4><p>至少两种实现方案：</p>
<ul>
<li><strong>手工编码实现法</strong><ul>
<li>相对复杂、且容易出错</li>
<li>但是目前非常流行的实现方法<ul>
<li>GCC, LLVM, …</li>
</ul>
</li>
</ul>
</li>
<li><strong>词法分析器的生成器</strong><ul>
<li>可快速原型、代码量较少</li>
<li>但较难控制细节</li>
</ul>
</li>
</ul>
<h4 id="转移图-手工编码的实现法"><a href="#转移图-手工编码的实现法" class="headerlink" title="转移图(手工编码的实现法)"></a>转移图(手工编码的实现法)</h4><p><img src="https://img.rath02.top/2026/01/image-20260122144357002" alt="image-20260122144357002"></p>
<p>图例符号解释：</p>
<ul>
<li><strong>单圆圈 (如 0, 1, 6)</strong>：中间状态，表示尚未完成一个完整单词的识别。</li>
<li><strong>双圆圈 (如 2, 3, 5, 7)</strong>：接受状态（Accepting State），表示已成功识别出一个记号（Token）。</li>
<li><strong>带星号的双圆圈 (4*, 8*)</strong>：表示在识别出当前 Token 时，多读了一个不属于该 Token 的字符（即 <code>other</code>），需要将读写指针<strong>回退（Retract）</strong>一位。</li>
<li><strong>return(relop, XXX)</strong>：词法分析器的返回结果，包含记号类型（relop）及其属性值（如 LE, EQ 等）。</li>
</ul>
<p>分支路径与识别结果（0表示初始状态，根据输入的第一个字符跳转不同的分支）：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th><strong>路径顺序 (状态转移)</strong></th>
<th><strong>输入字符</strong></th>
<th><strong>最终状态</strong></th>
<th><strong>识别出的运算符</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>0 → 1 → 2</td>
<td><code>&lt;</code> 然后 <code>=</code></td>
<td><strong>2</strong></td>
<td><code>LE</code> (&lt;=)</td>
<td>小于等于</td>
</tr>
<tr>
<td>0 → 1 → 3</td>
<td><code>&lt;</code> 然后 <code>&gt;</code></td>
<td><strong>3</strong></td>
<td><code>NE</code> (&lt;&gt;)</td>
<td>不等于 (Pascal风格)</td>
</tr>
<tr>
<td>0 → 1 → 4</td>
<td><code>&lt;</code> 然后 <code>other</code></td>
<td><strong>4*</strong></td>
<td><code>LT</code> (&lt;)</td>
<td>小于。<code>*</code> 表示需要回退一个字符</td>
</tr>
<tr>
<td>0 → 5</td>
<td><code>=</code></td>
<td><strong>5</strong></td>
<td><code>EQ</code> (=)</td>
<td>等于</td>
</tr>
<tr>
<td>0 → 6 → 7</td>
<td><code>&gt;</code> 然后 <code>=</code></td>
<td><strong>7</strong></td>
<td><code>GE</code> (&gt;=)</td>
<td>大于等于</td>
</tr>
<tr>
<td>0 → 6 → 8</td>
<td><code>&gt;</code> 然后 <code>other</code></td>
<td><strong>8*</strong></td>
<td><code>GT</code> (&gt;)</td>
<td>大于。<code>*</code> 表示需要回退一个字符</td>
</tr>
</tbody>
</table>
</div>
<h4 id="转移图算法"><a href="#转移图算法" class="headerlink" title="转移图算法"></a>转移图算法</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">token <span class="title function_">nextToken</span> <span class="params">()</span> &#123;</span><br><span class="line">    c = getChar ();</span><br><span class="line">    <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&lt;&#x27;</span>: c = getChar ();</span><br><span class="line">            <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;=&#x27;</span>: <span class="keyword">return</span> LE;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;&gt;&#x27;</span>: <span class="keyword">return</span> NE;</span><br><span class="line">                <span class="keyword">default</span>: rollback(); <span class="keyword">return</span> LT; <span class="comment">// 对应图中带 * 的状态 4</span></span><br><span class="line">            &#125;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;=&#x27;</span>: <span class="keyword">return</span> EQ;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&gt;&#x27;</span>: c = nextChar ();</span><br><span class="line">            <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="string">&#x27;=&#x27;</span>: <span class="keyword">return</span> GE;</span><br><span class="line">                <span class="keyword">default</span>: rollback(); <span class="keyword">return</span> GT; <span class="comment">// 对应图中带 * 的状态 8</span></span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="标识符的转移图"><a href="#标识符的转移图" class="headerlink" title="标识符的转移图"></a>标识符的转移图</h4><p><img src="https://img.rath02.top/2026/01/image-20260122150025526" alt="image-20260122150025526"></p>
<p>针对标识符（如变量名），其识别逻辑如下：</p>
<ul>
<li><p><strong>状态转移</strong>：</p>
<ol>
<li>从状态 0 开始，若输入为字母或下划线 <code>[a-zA-Z_]</code>，进入状态 1。</li>
<li>在状态 1 持续读入字母、数字或下划线 <code>[a-zA-Z0-9_]</code> 并自循环。</li>
<li>遇到其他字符（other）时进入状态 2，执行回退操作并返回 <strong>ID</strong>。</li>
</ol>
</li>
<li><p><strong>代码实现</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">token <span class="title function_">nextToken</span><span class="params">()</span> &#123;</span><br><span class="line">    c = getChar();</span><br><span class="line">    <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">            <span class="comment">// ... 其他 cases ...</span></span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;a&#x27;</span>, ..., <span class="string">&#x27;z&#x27;</span>, <span class="string">&#x27;A&#x27;</span>, ..., <span class="string">&#x27;Z&#x27;</span>, <span class="string">&#x27;_&#x27;</span>:</span><br><span class="line">            c = getChar();</span><br><span class="line">            <span class="comment">// 循环读取后续的字母、数字或下划线</span></span><br><span class="line">            <span class="keyword">while</span> (c == <span class="string">&#x27;a&#x27;</span> || c == <span class="string">&#x27;b&#x27;</span> || ... || c == <span class="string">&#x27;0&#x27;</span> || ... || c == <span class="string">&#x27;_&#x27;</span>) &#123;</span><br><span class="line">                c = getChar();</span><br><span class="line">            &#125;</span><br><span class="line">            rollback(); <span class="comment">// 遇到非标识符字符，执行回退</span></span><br><span class="line">            <span class="keyword">return</span> (ID); <span class="comment">// 返回标识符记号</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="标识符和关键字"><a href="#标识符和关键字" class="headerlink" title="标识符和关键字"></a>标识符和关键字</h4><p>许多语言中标识符和关键字有交集。从词法分析的角度看，<strong>关键字是标识符的一部分</strong>。</p>
<p>C语言为例的定义：</p>
<ul>
<li><strong>标识符：</strong> 以字母或下划线开头，后跟零个或多个字母、下划线、或数字。</li>
<li><strong>关键字：</strong> <code>if</code>, <code>while</code>, <code>else</code>, …</li>
</ul>
<h4 id="识别关键字-以if为例"><a href="#识别关键字-以if为例" class="headerlink" title="识别关键字(以if为例)"></a>识别关键字(以if为例)</h4><p><img src="https://img.rath02.top/2026/01/image-20260122203519384" alt="image-20260122203519384"></p>
<p>添加一条路径，在读到 <code>i</code> 后特别关注下一个字符是不是 <code>f</code>，如果是<code>f</code>，后续还要确保f后面不能有多余的字符，从而区分<code>if</code>关键词和其它标识符。</p>
<h4 id="关键字表算法"><a href="#关键字表算法" class="headerlink" title="关键字表算法"></a>关键字表算法</h4><ul>
<li>对给定语言中所有的关键字，构建关键字构成的哈希表H</li>
<li>对所有的标识符和关键字，先统一按标识符的转移图进行识别</li>
<li>识别完成后，进一步查表H看是否是关键字</li>
<li>通过合理的构造哈希表H(完美哈希)，可以O(1)时间完成</li>
</ul>
]]></content>
      <categories>
        <category>计算机科学与技术</category>
      </categories>
      <tags>
        <tag>编译原理</tag>
        <tag>底层逻辑</tag>
      </tags>
  </entry>
  <entry>
    <title>高数基础</title>
    <url>/2026/01/20/%E9%AB%98%E6%95%B0%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="第一部分：基本逻辑"><a href="#第一部分：基本逻辑" class="headerlink" title="第一部分：基本逻辑"></a>第一部分：基本逻辑</h1><h2 id="1-命题逻辑关系"><a href="#1-命题逻辑关系" class="headerlink" title="1. 命题逻辑关系"></a>1. 命题逻辑关系</h2><p>设命题 $A,B$：</p>
<ul>
<li><strong>充分条件</strong>：<script type="math/tex; mode=display">
A \Rightarrow B</script>有 $A$ 一定有 $B$。</li>
<li><strong>必要条件</strong>：<script type="math/tex; mode=display">
B \Rightarrow A</script>没有 $B$ 就一定没有 $A$。</li>
<li><strong>充要条件</strong>：<script type="math/tex; mode=display">
A \Leftrightarrow B</script>$A$ 与 $B$ 等价。</li>
<li><strong>无关条件</strong>：<br>$A$ 与 $B$ 互不推出。</li>
</ul>
<p>注意：<br>“$A$ 是 $B$ 的充分不必要条件”与“$A$ 是 $B$ 的必要不充分条件”<strong>方向不同，不能混淆</strong>。</p>
<hr>
<h2 id="2-逆否命题"><a href="#2-逆否命题" class="headerlink" title="2. 逆否命题"></a>2. 逆否命题</h2><p>命题</p>
<script type="math/tex; mode=display">
\text{若 } A \text{，则 } B</script><p>等价于其逆否命题</p>
<script type="math/tex; mode=display">
\text{若非 } B \text{，则非 } A</script><p>在证明“若 $A$ 则 $B$”较困难时，<strong>优先考虑证明逆否命题</strong>。</p>
<hr>
<h2 id="3-德·摩根律（对偶律）"><a href="#3-德·摩根律（对偶律）" class="headerlink" title="3. 德·摩根律（对偶律）"></a>3. 德·摩根律（对偶律）</h2><p>集合或逻辑中的对立转化：</p>
<script type="math/tex; mode=display">
\overline{A \cup B} = \overline{A} \cap \overline{B}</script><script type="math/tex; mode=display">
\overline{A \cap B} = \overline{A} \cup \overline{B}</script><p>口诀：<br><strong>长杠变短杠，开口换方向</strong>。</p>
<hr>
<h2 id="4-数学归纳法"><a href="#4-数学归纳法" class="headerlink" title="4. 数学归纳法"></a>4. 数学归纳法</h2><p>适用于与自然数 $n$ 有关的命题。</p>
<p>步骤：</p>
<ol>
<li><strong>验</strong>：验证 $n = n_0$ 时命题成立</li>
<li><strong>设</strong>：假设 $n = k$ 时命题成立</li>
<li><strong>证</strong>：在假设成立的基础上，证明 $n = k + 1$ 也成立</li>
</ol>
<p>注意：<br>“设 $n=k$ 成立”<strong>只是推理前提，不能当结论使用</strong>。</p>
<hr>
<h1 id="第二部分：解析式的概念与运算"><a href="#第二部分：解析式的概念与运算" class="headerlink" title="第二部分：解析式的概念与运算"></a>第二部分：解析式的概念与运算</h1><h2 id="1-解析式分类"><a href="#1-解析式分类" class="headerlink" title="1. 解析式分类"></a>1. 解析式分类</h2><ul>
<li><strong>代数式</strong>：整式、分式、无理式</li>
<li><strong>超越式</strong>：指数式、对数式、三角函数式、反三角函数式</li>
</ul>
<p>一般指可由有限次代数运算和初等函数运算表示的式子。</p>
<hr>
<h2 id="2-整式"><a href="#2-整式" class="headerlink" title="2. 整式"></a>2. 整式</h2><ul>
<li><strong>单项式</strong>：系数与字母幂的乘积</li>
<li><strong>多项式</strong>：若干单项式的代数和</li>
</ul>
<p>次数判定：</p>
<ul>
<li>单项式次数：各字母指数之和</li>
<li>多项式次数：各项中次数的最大值</li>
</ul>
<p>多项式乘法推荐使用 <strong>系数竖式法</strong>，可有效减少漏项。</p>
<hr>
<h2 id="3-分式与多项式长除法"><a href="#3-分式与多项式长除法" class="headerlink" title="3. 分式与多项式长除法"></a>3. 分式与多项式长除法</h2><p>化简步骤：</p>
<ol>
<li>分解因式，约去公因式</li>
<li>若分子次数 $\ge$ 分母次数，可使用 <strong>多项式长除法</strong></li>
</ol>
<p>多项式长除法常用于：</p>
<ul>
<li>分式化简</li>
<li>因式分解</li>
<li>研究函数渐近行为</li>
</ul>
<hr>
<h2 id="4-无理式与根式"><a href="#4-无理式与根式" class="headerlink" title="4. 无理式与根式"></a>4. 无理式与根式</h2><p>$n$ 次算术根定义：</p>
<ul>
<li>当 $n$ 为偶数时，$\sqrt[n]{a}$ 表示 <strong>唯一的非负实数</strong></li>
</ul>
<p>重要性质：</p>
<script type="math/tex; mode=display">
\sqrt{a^2} = |a|</script><p>（非常容易漏掉绝对值）</p>
<p>常见技巧：</p>
<ul>
<li>分母有理化</li>
<li>分子有理化</li>
<li>共轭根式的使用</li>
</ul>
<hr>
<h1 id="第三部分：方程与不等式"><a href="#第三部分：方程与不等式" class="headerlink" title="第三部分：方程与不等式"></a>第三部分：方程与不等式</h1><h2 id="1-一元二次方程"><a href="#1-一元二次方程" class="headerlink" title="1. 一元二次方程"></a>1. 一元二次方程</h2><p>标准形式：</p>
<script type="math/tex; mode=display">
ax^2 + bx + c = 0 \quad (a \ne 0)</script><p>求根公式：</p>
<script type="math/tex; mode=display">
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}</script><p>判别式：</p>
<script type="math/tex; mode=display">
\Delta = b^2 - 4ac</script><ul>
<li>$\Delta &gt; 0$：两个不等实根</li>
<li>$\Delta = 0$：重根</li>
<li>$\Delta &lt; 0$：一对共轭复根（含 $i$）</li>
</ul>
<hr>
<h2 id="2-韦达定理"><a href="#2-韦达定理" class="headerlink" title="2. 韦达定理"></a>2. 韦达定理</h2><p>对于方程 $ax^2 + bx + c = 0$，设两根为 $x_1, x_2$：</p>
<script type="math/tex; mode=display">
x_1 + x_2 = -\frac{b}{a}</script><script type="math/tex; mode=display">
x_1 x_2 = \frac{c}{a}</script><p>可推广至 $n$ 次方程，但考试中以二次最常见。</p>
<hr>
<h2 id="3-常用不等式"><a href="#3-常用不等式" class="headerlink" title="3. 常用不等式"></a>3. 常用不等式</h2><h3 id="均值不等式"><a href="#均值不等式" class="headerlink" title="均值不等式"></a>均值不等式</h3><script type="math/tex; mode=display">
\text{调和平均} \le \text{几何平均} \le \text{算术平均} \le \text{均方根}</script><p>使用前需满足 <strong>非负条件</strong>。</p>
<hr>
<h3 id="三角不等式"><a href="#三角不等式" class="headerlink" title="三角不等式"></a>三角不等式</h3><script type="math/tex; mode=display">
|a + b| \le |a| + |b|</script><p>等号成立条件：$ab \ge 0$。</p>
<hr>
<h3 id="柯西不等式"><a href="#柯西不等式" class="headerlink" title="柯西不等式"></a>柯西不等式</h3><script type="math/tex; mode=display">
(a_1^2 + a_2^2 + \cdots + a_n^2)(b_1^2 + b_2^2 + \cdots + b_n^2)
\ge
(a_1 b_1 + a_2 b_2 + \cdots + a_n b_n)^2</script><p>常用于代数放缩与最值问题。</p>
<hr>
<h2 id="4-二次函数研究"><a href="#4-二次函数研究" class="headerlink" title="4. 二次函数研究"></a>4. 二次函数研究</h2><p>通过函数图像分析：</p>
<ul>
<li>根是否在某区间内</li>
<li>根在某常数左右的条件</li>
</ul>
<p>常见问题类型：</p>
<ul>
<li>区间内恒成立</li>
<li>区间最值</li>
<li>参数取值范围讨论</li>
</ul>
<hr>
<h1 id="第四部分：函数"><a href="#第四部分：函数" class="headerlink" title="第四部分：函数"></a>第四部分：函数</h1><h2 id="1-基本初等函数"><a href="#1-基本初等函数" class="headerlink" title="1. 基本初等函数"></a>1. 基本初等函数</h2><p>幂函数、指数函数、对数函数需掌握：</p>
<ul>
<li>定义域</li>
<li>单调性</li>
<li>图像特征</li>
</ul>
<p>对数运算法则：</p>
<script type="math/tex; mode=display">
\log(ab) = \log a + \log b</script><script type="math/tex; mode=display">
\log\left(\frac{a}{b}\right) = \log a - \log b</script><script type="math/tex; mode=display">
\log(a^k) = k \log a</script><hr>
<h2 id="2-三角与反三角函数"><a href="#2-三角与反三角函数" class="headerlink" title="2. 三角与反三角函数"></a>2. 三角与反三角函数</h2><p>基本三角函数：</p>
<script type="math/tex; mode=display">
\sin x,\quad \cos x,\quad \tan x</script><p>反三角函数需特别注意：</p>
<ul>
<li>定义域</li>
<li>主值区间</li>
</ul>
<p>诱导公式口诀：<br><strong>奇变偶不变，符号看象限</strong>。</p>
<hr>
<h2 id="3-图像变换"><a href="#3-图像变换" class="headerlink" title="3. 图像变换"></a>3. 图像变换</h2><p>平移规律：</p>
<ul>
<li>左加右减</li>
<li>上加下减</li>
</ul>
<p>伸缩变换：</p>
<ul>
<li>水平伸缩：影响 $x$</li>
<li>垂直伸缩：影响 $y$</li>
</ul>
<hr>
<h2 id="4-函数特性"><a href="#4-函数特性" class="headerlink" title="4. 函数特性"></a>4. 函数特性</h2><ul>
<li><strong>奇函数</strong>：关于原点对称</li>
<li><strong>偶函数</strong>：关于 $y$ 轴对称</li>
</ul>
<p>对称性常用于简化最值和图像分析。</p>
<hr>
<h1 id="第五部分：数列及其单调性"><a href="#第五部分：数列及其单调性" class="headerlink" title="第五部分：数列及其单调性"></a>第五部分：数列及其单调性</h1><h2 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1. 基本概念"></a>1. 基本概念</h2><p>数列是定义在正整数集上的函数：</p>
<script type="math/tex; mode=display">
a_1, a_2, \ldots, a_n, \ldots</script><hr>
<h2 id="2-等差与等比数列"><a href="#2-等差与等比数列" class="headerlink" title="2. 等差与等比数列"></a>2. 等差与等比数列</h2><h3 id="等差数列"><a href="#等差数列" class="headerlink" title="等差数列"></a>等差数列</h3><p>通项公式：</p>
<script type="math/tex; mode=display">
a_n = a_1 + (n-1)d</script><p>前 $n$ 项和：</p>
<script type="math/tex; mode=display">
S_n = \frac{n(a_1 + a_n)}{2}</script><hr>
<h3 id="等比数列"><a href="#等比数列" class="headerlink" title="等比数列"></a>等比数列</h3><p>通项公式：</p>
<script type="math/tex; mode=display">
a_n = a_1 q^{,n-1}</script><p>当 $q \ne 1$ 时，求和常用 <strong>错位相减法</strong>。</p>
<hr>
<h2 id="3-数列单调性"><a href="#3-数列单调性" class="headerlink" title="3. 数列单调性"></a>3. 数列单调性</h2><p>比较相邻两项：</p>
<script type="math/tex; mode=display">
a_{n+1} - a_n</script><ul>
<li>$&gt;0$：递增</li>
<li>$&lt;0$：递减</li>
</ul>
<hr>
<h2 id="4-常用技巧"><a href="#4-常用技巧" class="headerlink" title="4. 常用技巧"></a>4. 常用技巧</h2><ul>
<li>通项与和的关系：<script type="math/tex; mode=display">
a_n = S_n - S_{n-1}</script></li>
<li>裂项相消</li>
<li>乘除相消</li>
</ul>
<hr>
<h1 id="第六部分：坐标系及其变换"><a href="#第六部分：坐标系及其变换" class="headerlink" title="第六部分：坐标系及其变换"></a>第六部分：坐标系及其变换</h1><h2 id="1-直角坐标系变换"><a href="#1-直角坐标系变换" class="headerlink" title="1. 直角坐标系变换"></a>1. 直角坐标系变换</h2><h3 id="平移变换"><a href="#平移变换" class="headerlink" title="平移变换"></a>平移变换</h3><p>根据新旧原点之间的关系调整坐标。</p>
<hr>
<h3 id="旋转变换"><a href="#旋转变换" class="headerlink" title="旋转变换"></a>旋转变换</h3><p>坐标轴绕原点逆时针旋转 $\theta$：</p>
<script type="math/tex; mode=display">
\begin{cases}
x = x' \cos \theta - y' \sin \theta \
y = x' \sin \theta + y' \cos \theta
\end{cases}</script><hr>
<h2 id="2-极坐标系"><a href="#2-极坐标系" class="headerlink" title="2. 极坐标系"></a>2. 极坐标系</h2><p>组成要素：</p>
<ul>
<li>极点</li>
<li>极轴</li>
<li>极径 $r$</li>
<li>极角 $\theta$</li>
</ul>
<p>通常研究狭义极坐标：</p>
<script type="math/tex; mode=display">
r \ge 0,\quad \theta \in [0, 2\pi)</script><hr>
<h2 id="3-坐标互化"><a href="#3-坐标互化" class="headerlink" title="3. 坐标互化"></a>3. 坐标互化</h2><p>桥梁公式：</p>
<script type="math/tex; mode=display">
x = r \cos \theta,\quad y = r \sin \theta</script><script type="math/tex; mode=display">
r = \sqrt{x^2 + y^2}</script>]]></content>
      <categories>
        <category>数学</category>
      </categories>
      <tags>
        <tag>零基础</tag>
        <tag>高数</tag>
      </tags>
  </entry>
  <entry>
    <title>图床教程</title>
    <url>/2026/01/16/%E5%9B%BE%E5%BA%8A%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h3 id="推荐方案：Cloudflare-R2-PicGo"><a href="#推荐方案：Cloudflare-R2-PicGo" class="headerlink" title="推荐方案：Cloudflare R2 + PicGo"></a>推荐方案：Cloudflare R2 + PicGo</h3><p>这是目前最推荐给使用了 Cloudflare 用户的方案。</p>
<ul>
<li><strong>为什么选它？</strong><ol>
<li><strong>完全免费</strong>：Cloudflare R2 提供 <strong>10GB</strong> 的免费存储空间（对博客图片来说几乎用不完）和 <strong>零流量费</strong>。</li>
<li><strong>速度快</strong>：直接利用 Cloudflare 全球 CDN 加速。</li>
<li><strong>无需备案</strong>：你的域名在 spaceship 买的，DNS 在 Cloudflare，完全不需要经过国内备案流程。</li>
<li><strong>专业</strong>：你可以使用自己的二级域名（如 <code>img.你的域名</code>）作为图片链接。如果未来你想换图床，只需要改一下 DNS 解析，文章里的几百张图片链接<strong>完全不用动</strong>。</li>
</ol>
</li>
</ul>
<hr>
<h3 id="配置保姆级教程"><a href="#配置保姆级教程" class="headerlink" title="配置保姆级教程"></a>配置保姆级教程</h3><h4 id="第一步：在-Cloudflare-开启-R2-存储桶"><a href="#第一步：在-Cloudflare-开启-R2-存储桶" class="headerlink" title="第一步：在 Cloudflare 开启 R2 存储桶"></a>第一步：在 Cloudflare 开启 R2 存储桶</h4><ol>
<li>登录 <a href="https://www.google.com/search?q=https://dash.cloudflare.com/">Cloudflare Dashboard</a>。</li>
<li>在左侧菜单找到 <strong>R2</strong>（如果没开通，点击启用，需要绑定信用卡或 PayPal 验证，但<strong>不会扣费</strong>，只要你没超过 10GB）。</li>
<li>点击 <strong>“Create bucket” (创建存储桶)</strong>。<ul>
<li><strong>Bucket name</strong>: 随便起，比如 <code>blog-imgs</code>。</li>
<li><strong>Location</strong>: 建议选 <code>Automatic</code> 或 <code>North America</code> (因为你的博客在 GitHub Pages，美西节点连接顺畅)。</li>
<li>点击 <strong>Create bucket</strong>。</li>
</ul>
</li>
</ol>
<h4 id="第二步：绑定你的域名-关键"><a href="#第二步：绑定你的域名-关键" class="headerlink" title="第二步：绑定你的域名 (关键)"></a>第二步：绑定你的域名 (关键)</h4><ol>
<li>进入刚才创建的 bucket 页面。</li>
<li>点击顶部的 <strong>“Settings” (设置)</strong> 标签。</li>
<li>向下滚动找到 <strong>“Public Access” (公开访问)</strong> -&gt; <strong>“Custom Domains” (自定义域)</strong>。</li>
<li>点击 <strong>“Connect Domain”</strong>。</li>
<li>输入你想用的二级域名，比如 <code>img.你的域名</code> (前提是你的主域名 DNS 确实是在 Cloudflare 管理的)。</li>
<li>点击 <strong>Continue</strong> -&gt; <strong>Connect Domain</strong>。</li>
<li>等待几分钟，状态显示 “Active” 后，以后你的图片就可以通过 <code>https://img.你的域名/图片名.png</code> 访问了。</li>
</ol>
<span id="more"></span>
<h4 id="第三步：获取-API-密钥"><a href="#第三步：获取-API-密钥" class="headerlink" title="第三步：获取 API 密钥"></a>第三步：获取 API 密钥</h4><p>为了让 PicGo 能上传图片，你需要“钥匙”。</p>
<ol>
<li>回到 R2 的主界面（列出所有 bucket 的那个页面）。</li>
<li>在右侧找到 <strong>“Manage R2 API Tokens”</strong>。</li>
<li>点击 <strong>“Create API Token”</strong>。</li>
<li><strong>Permissions</strong>: 选择 <strong>Admin Read &amp; Write</strong> (读写权限)。</li>
<li><strong>TTL</strong>: 选 <code>Forever</code> (永久)。</li>
<li>点击 <strong>Create API Token</strong>。</li>
<li><strong>重要：</strong> 屏幕上会显示 <code>Access Key ID</code> 和 <code>Secret Access Key</code>，<strong>立刻复制保存下来</strong>，这个页面关了就再也看不到了。</li>
</ol>
<h4 id="第四步：配置-PicGo"><a href="#第四步：配置-PicGo" class="headerlink" title="第四步：配置 PicGo"></a>第四步：配置 PicGo</h4><p>PicGo 原生不支持 R2，需要安装一个插件（因为 R2 兼容 AWS S3 协议）。</p>
<ol>
<li>打开 <strong>PicGo</strong> -&gt; <strong>插件设置</strong>。</li>
<li>搜索安装 <code>picgo-plugin-s3</code> (注意是 s3，不是 aws-s3，推荐用 <code>s3</code> 插件更轻量，或者 <code>picgo-plugin-aws-s3</code> 也可以)。<ul>
<li><em>如果安装失败，可能需要先安装 Node.js，或者多试几次。</em></li>
</ul>
</li>
<li>安装好后，点击 <strong>图床设置</strong> -&gt; <strong>Amazon S3</strong> (插件装好后会出现这个选项)。</li>
<li><strong>填入配置：</strong><ul>
<li><strong>图床配置名</strong>: <code>R2</code> (随便填)</li>
<li><strong>应用密钥 ID (AccessKeyID)</strong>: 刚才复制的 Access Key ID。</li>
<li><strong>应用密钥 (SecretAccessKey)</strong>: 刚才复制的 Secret Access Key。</li>
<li><strong>桶名 (Bucket)</strong>: 你创建的 bucket 名字 (比如 <code>blog-imgs</code>)。</li>
<li><strong>文件路径</strong>: <code>{year}/{month}/{fileName}</code> (可选，按年月分类)。</li>
<li><strong>地区 (Region)</strong>: <code>auto</code> (如果你一定要填，填 <code>us-east-1</code>，但在 Cloudflare R2 中通常填 <code>auto</code> 或留空)。</li>
<li><strong>自定义节点 (Endpoint)</strong>: <strong>这是关键！</strong><ul>
<li>回到 Cloudflare R2 界面，在 bucket 详情页，你会看到一个 <strong>“S3 API”</strong> 的链接，通常长这样：<code>https://&lt;你的账号ID&gt;.r2.cloudflarestorage.com</code>。</li>
<li><strong>把这个链接填进去。</strong> (注意：<strong>不要</strong>在后面加 bucket 名字)。</li>
</ul>
</li>
<li><strong>自定义域名</strong>: <code>https://img.你的域名/{path}</code> (刚才绑定的那个二级域名)。</li>
<li><strong>强制路径样式 (ForcePathStyle)</strong>: 选 <code>否</code> (或者如果不成功改选 <code>是</code> 试试)。</li>
</ul>
</li>
<li>点击 <strong>确定</strong> 并 <strong>设为默认图床</strong>。</li>
</ol>
<p><img src="https://img.rath02.top/2026/01/image-20260116213545933" alt="image-20260116213545933"></p>
]]></content>
      <categories>
        <category>教程</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>图床</tag>
      </tags>
  </entry>
  <entry>
    <title>Data Structure</title>
    <url>/2026/03/10/Data-Structure/</url>
    <content><![CDATA[<h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="1-绪论"><a href="#1-绪论" class="headerlink" title="1 绪论"></a>1 绪论</h2><h3 id="1-1-数据结构的基本概念"><a href="#1-1-数据结构的基本概念" class="headerlink" title="1.1 数据结构的基本概念"></a>1.1 数据结构的基本概念</h3><p>1.数据：数据就是<strong>信息的载体</strong>，是对客观事物属性的描述。</p>
<p>2.数据元素：数据的最小单位，通常由<strong>值域(存储的数据)</strong>和<strong>相关属性(标识符或下标)</strong>组成。</p>
<p>3.数据对象：有相同性质的数据元素的集合，形成一个整体。</p>
<p>4.数据类型：一个值的集合及其允许的操作，分为<strong>原子类型、结构类型和抽象数据类型(ADT)</strong>。</p>
<p>5.数据结构：相互之间存在一种或多种特定关系的数据元素的集合。包括<strong>逻辑结构、存储结构、和数据的运算</strong>。</p>
<h3 id="1-2-数据结构三要素"><a href="#1-2-数据结构三要素" class="headerlink" title="1.2 数据结构三要素"></a>1.2 数据结构三要素</h3><p>逻辑结构：定义了数据元素之间的逻辑关系，不考虑计算机真实的内存布局。它独立于存储结构，可分为<strong>线性结构和非线性结构</strong>。</p>
<p><img src="https://img.rath02.top/2026/03/image-20260312212822916" alt="image-20260312212822916"></p>
<p>四种基本结构关系如下图：</p>
<p><img src="https://img.rath02.top/2026/03/image-20260313000112997" alt="image-20260313000112997"></p>
<p>存储结构：在计算机内存中的具体映射。同样的算法设计用不同的存储实现效率完全不同。如栈可以用顺序存储(数组实现)，也可以使用链式存储(链表实现)。</p>
<p><strong>常见的存储结构有顺序存储、链式存储、索引存储和散列存储。</strong></p>
<blockquote>
<p>顺序存储：逻辑上相邻的数据元素存储在物理上相邻的存储单元中。优点是随机存储，时间复杂度为O(1)，缺点是容易产生外部碎片(要使用连续的存储单元)。</p>
<p>链式存储：不要求逻辑相邻的数据元素存储在物理上相邻的存储单元。使用指针指示元素的存储地址。优点是不会产生碎片，充分利用存储单元。缺点是每个元素存储指针而额外占用空间，而且只能顺序访问。</p>
<p>索引存储：在存储数据元素时，额外建立索引表。索引表的每一项称为索引项，包括关键字和地址。优点是检索速度快，缺点是需要额外存储空间用于索引表，在增加和删除元素时需要更新索引表，带来额外时间开销。</p>
<p>散列存储：根据元素的关键字直接计算出其存储地址，称HashMap。优点是检索、插入和删除操作都很快速，缺点是散列函数如果设计不当，可能产生冲突，解决冲突会增加时间和空间成本。</p>
</blockquote>
<p>数据的运算：<strong>包括定义和实现</strong>。定义说明运算的功能，实现完成运算的具体步骤。</p>
<h3 id="1-3-算法和算法评价"><a href="#1-3-算法和算法评价" class="headerlink" title="1.3 算法和算法评价"></a>1.3 算法和算法评价</h3><p>算法是对特定问题求解步骤的描述，是一个有穷指令序列。一个好的算法应该具有下面五个重要特性。</p>
<blockquote>
<p>有穷性：算法在有限步后结束，每一步在有限时间内完成。</p>
<p>确定性：算法每条指令有明确的含义，对于相同的输入，只能产生相同的输出。</p>
<p>可行性：算法描述的操作可以通过已实现的基本运算执行有限次数完成。</p>
<p>输入：算法可以有零个或多个输入。</p>
<p>输出：算法至少有一个输出。</p>
</blockquote>
<p>设计一个好的算法，应该满足以下目标：</p>
<ol>
<li>正确性</li>
<li>可读性</li>
<li>健壮性</li>
<li>高效率和低存储量需求</li>
</ol>
<p>算法效率的度量：在算法设计中，正确性是基础，效率是衡量算法优劣的关键指标。我们引入数学中渐近复杂度分析——通过<strong>时间复杂度和空间复杂度</strong>来描述算法随<strong>问题规模</strong>增长的资源消耗趋势。</p>
<p><strong>时间复杂度</strong>：用大O记法，不关注算法所用的时间，只关注其所用的步数。它反映的是当数据量(问题规模)增大时，步数如何变化。通常只要关注大O中增长最快的项，且考虑算法的最坏情况。</p>
<p><strong>常见的时间复杂度阶次</strong></p>
<script type="math/tex; mode=display">O(1) < O(\log n) < O(n) < O(n \log n) < O(n^2) < O(n^3) < O(2^n) < O(n!)</script><p>暴力的算法最容易想到，但是时间复杂度较高。应该考虑用大O的思想去优化，写出更高效率的代码。</p>
<p><strong>空间复杂度</strong>：表示算法在运行过程中所需要的<strong>额外存储空间</strong>，若算法只用常量级的额外空间，空间复杂度为O(1)。</p>
<h3 id="1-4-时间复杂度题目分析"><a href="#1-4-时间复杂度题目分析" class="headerlink" title="1.4 时间复杂度题目分析"></a>1.4 时间复杂度题目分析</h3><p>法一：建立<strong>基本运算的执行次数x</strong>与<strong>问题规模n</strong>之间的关系式。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> i = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span>(i &lt;= n)&#123;</span><br><span class="line">    i = i * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>循环执行次数等于 <code>i</code> 从 1 以<strong>乘 2</strong>的方式增长，直到 <code>i &gt; n</code>。</p>
<ul>
<li>第 1 次：i = 1 → 2</li>
<li>第 2 次：i = 2 → 4</li>
<li>第 3 次：i = 4 → 8</li>
<li>…</li>
<li>第 k 次：i = 2^(k-1) → 2^k</li>
</ul>
<p>循环结束条件：<code>2^k &gt; n</code></p>
<p><script type="math/tex">k = \lceil \log_2 n \rceil</script>，所以算法时间复杂度为<script type="math/tex">T(n) = O(\log_2 n)</script>。</p>
<p>法二：用数学归纳法或直接累计循环次数，对于递归关系式可以建立递推关系求解。</p>
<p>法三：邪修方法，设n=64，模拟程序运行过程，带入大O的式子判断时间复杂度。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">binarySearch</span><span class="params">(<span class="type">int</span> arr[], <span class="type">int</span> n, <span class="type">int</span> key)</span>&#123;</span><br><span class="line">    <span class="type">int</span> l = <span class="number">0</span>, r = n<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt;= r)&#123;</span><br><span class="line">        <span class="type">int</span> mid = (l+r)/<span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(arr[mid] == key) <span class="keyword">return</span> mid;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(arr[mid] &lt; key) l = mid+<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> r = mid<span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>步骤</th>
<th>说明</th>
<th>n=64时模拟</th>
<th>循环次数/操作次数</th>
<th>规律分析</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>初始化</td>
<td><code>l=0, r=63</code></td>
<td>0</td>
<td>无循环，初始化</td>
</tr>
<tr>
<td>2</td>
<td>第1次循环</td>
<td><code>mid=(0+63)/2=31</code></td>
<td>1</td>
<td>查找范围减半</td>
</tr>
<tr>
<td>3</td>
<td>第2次循环</td>
<td><code>mid=(32+63)/2=47</code></td>
<td>2</td>
<td>再减半</td>
</tr>
<tr>
<td>4</td>
<td>第3次循环</td>
<td><code>mid=(32+46)/2=39</code></td>
<td>3</td>
<td>减半</td>
</tr>
<tr>
<td>5</td>
<td>第4次循环</td>
<td><code>mid=(32+38)/2=35</code></td>
<td>4</td>
<td>减半</td>
</tr>
<tr>
<td>6</td>
<td>第5次循环</td>
<td><code>mid=(32+34)/2=33</code></td>
<td>5</td>
<td>减半</td>
</tr>
<tr>
<td>7</td>
<td>第6次循环</td>
<td><code>mid=(34+34)/2=34</code></td>
<td>6</td>
<td>减半</td>
</tr>
<tr>
<td>8</td>
<td>第7次循环</td>
<td><code>l&gt;r</code>，循环结束</td>
<td>6</td>
<td>循环退出</td>
</tr>
</tbody>
</table>
</div>
<p>6显然和<script type="math/tex">\log_2 64</script>的结果比较近，于是<script type="math/tex">T(n) = O(\log_2 n)</script>。实际在做题的时候，模拟得到<strong>关键语句</strong>执行次数就行。</p>
<h3 id="1-5-斐波那契数列"><a href="#1-5-斐波那契数列" class="headerlink" title="1.5 斐波那契数列"></a>1.5 斐波那契数列</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">fib</span><span class="params">(<span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) <span class="keyword">return</span> n;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">fib</span>(n<span class="number">-1</span>) + <span class="built_in">fib</span>(n<span class="number">-2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>分析步骤：</p>
<ol>
<li>建立递推关系：<br>设 T(n) 为 <code>fib(n)</code> 的基本操作次数：<br>T(n) = T(n-1) + T(n-2) + 1<br>其中 +1 表示本层加法操作。</li>
<li>展开递归：<br>T(n) = T(n-1) + T(n-2) + 1<br>= (T(n-2) + T(n-3) + 1) + T(n-2) + 1<br>= 2T(n-2) + T(n-3) + 2<br>继续展开，可以看到执行次数呈指数增长。</li>
<li>时间复杂度：<br>T(n) = O(2^n)</li>
</ol>
<p>非递归实现（迭代法）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">fib_iter</span><span class="params">(<span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(n &lt;= <span class="number">1</span>) <span class="keyword">return</span> n;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">0</span>, b = <span class="number">1</span>, c;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++)&#123;</span><br><span class="line">        c = a + b;</span><br><span class="line">        a = b;</span><br><span class="line">        b = c;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>分析步骤：</p>
<ol>
<li>循环次数：<br>从 2 到 n → 循环次数 = n-1</li>
<li>每次循环基本操作：<br>加法 c = a + b、赋值 a = b、赋值 b = c → 3 次<br>总操作次数 x ≈ 3(n-1)</li>
<li>时间复杂度：<br>T(n) = O(n)</li>
</ol>
<h2 id="2-线性表"><a href="#2-线性表" class="headerlink" title="2 线性表"></a>2 线性表</h2><h3 id="2-1-线性表的定义和操作"><a href="#2-1-线性表的定义和操作" class="headerlink" title="2.1 线性表的定义和操作"></a>2.1 线性表的定义和操作</h3>]]></content>
      <categories>
        <category>考研</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>王道</tag>
      </tags>
  </entry>
</search>
